<Window x:Class="Kurs4.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Учёт поступления товаров" 
        Height="760" Width="1410"
        WindowStartupLocation="CenterScreen"
        Background="#F5F7FA">

    <Window.Resources>
        <Style x:Key="MenuButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="180"/>
            <Setter Property="Height" Value="180"/>
            <Setter Property="Margin" Value="15"/>
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                        <GradientStop Color="#4A90E2" Offset="0.0"/>
                        <GradientStop Color="#357ABD" Offset="1.0"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Border x:Name="border" 
                                    Background="{TemplateBinding Background}" 
                                    CornerRadius="20"
                                    SnapsToDevicePixels="True">
                                <Border.Effect>
                                    <DropShadowEffect BlurRadius="15" 
                                                      ShadowDepth="2" 
                                                      Opacity="0.3" 
                                                      Color="#2C3E50"/>
                                </Border.Effect>
                                <StackPanel VerticalAlignment="Center" 
                                            HorizontalAlignment="Center">
                                    <TextBlock x:Name="icon" 
                                               FontFamily="Segoe MDL2 Assets" 
                                               FontSize="48" 
                                               Text="{TemplateBinding Tag}" 
                                               HorizontalAlignment="Center"/>
                                    <TextBlock Text="{TemplateBinding Content}" 
                                               Margin="0,10,0,0" 
                                               HorizontalAlignment="Center"/>
                                </StackPanel>
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" TargetName="border">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                            <GradientStop Color="#357ABD" Offset="0.0"/>
                                            <GradientStop Color="#2C5F8A" Offset="1.0"/>
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="RenderTransform" TargetName="border">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.98" ScaleY="0.98"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="HeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="36"/>
            <Setter Property="FontWeight" Value="Light"/>
            <Setter Property="Foreground" Value="#2C3E50"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                <GradientStop Color="#E8ECF2" Offset="0.0"/>
                <GradientStop Color="#D9E0E9" Offset="1.0"/>
            </LinearGradientBrush>
        </Grid.Background>

        <StackPanel VerticalAlignment="Center" >
            <TextBlock Text="Учёт поступления товаров" 
                       Style="{StaticResource HeaderStyle}"/>
            <TextBlock Text="Складской учёт и аналитика" 
                       FontSize="18" 
                       Foreground="#7F8C8D" 
                       HorizontalAlignment="Center" 
                       Margin="0,5,0,40"/>

            <UniformGrid Columns="3" Rows="2" HorizontalAlignment="Center">
                <Button Style="{StaticResource MenuButtonStyle}" 
                        Tag="&#xE710;"   
                        Content="Поступление"
                        Click="btnPriem"
                        />

                <Button Style="{StaticResource MenuButtonStyle}" 
                        Tag="&#xE71D;"   
                        Content="Товары на складе" Click="Button_Click_2"/>

                <Button Style="{StaticResource MenuButtonStyle}" 
                        Tag="&#xE72E;"   
                        Content="Отгрузка" Click="Button_Click_1"/>

                <Button Style="{StaticResource MenuButtonStyle}" 
                        Tag="&#xE713;"   
                        Content="Справочники" Click="Button_Click_3"/>

                <Button Style="{StaticResource MenuButtonStyle}" 
                        Tag="&#xE711;"   
                        Content="Выход" Click="Button_Click"/>
            </UniformGrid>

            <StatusBar VerticalAlignment="Bottom" 
                       Background="Transparent" 
                       Margin="0,50,0,0">
                <TextBlock Text="База данных: SQL Server (Dionnected)" 
                           Foreground="Red" 
                           FontSize="14"/>

                <!--"#27AE60" активное соединение-->
            </StatusBar>
        </StackPanel>
    </Grid>
</Window>